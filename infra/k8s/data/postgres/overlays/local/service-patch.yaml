apiVersion: v1
kind: Service
metadata:
  name: postgres
spec:
  # Optional local access via `minikube service postgres -n control-panel`.
  type: NodePort
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
      nodePort: 30432
      protocol: TCP
