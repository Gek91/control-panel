apiVersion: v1
kind: Secret
metadata:
  name: postgres
  labels:
    app.kubernetes.io/name: postgres
    app.kubernetes.io/component: database
    app.kubernetes.io/part-of: control-panel
type: Opaque
stringData:
  # Local/minikube only — replace for non-local environments.
  POSTGRES_PASSWORD: controlpanel
