apiVersion: apps/v1
kind: Deployment
metadata:
  name: cash-manager-be
  labels:
    app.kubernetes.io/name: cash-manager-be
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: control-panel
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: cash-manager-be
  template:
    metadata:
      labels:
        app.kubernetes.io/name: cash-manager-be
        app.kubernetes.io/component: backend
        app.kubernetes.io/part-of: control-panel
    spec:
      containers:
        - name: cash-manager-be
          image: cash-manager:0.0.1-SNAPSHOT
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          envFrom:
            - configMapRef:
                name: cash-manager-be
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 20
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 1000
            capabilities:
              drop:
                - ALL
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
      securityContext:
        seccompProfile:
          type: RuntimeDefault
