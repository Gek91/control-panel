apiVersion: v1
kind: ConfigMap
metadata:
  name: cash-manager-be
  labels:
    app.kubernetes.io/name: cash-manager-be
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: control-panel
data:
  SERVER_PORT: "8080"
  # Default Spring profile (embedded H2 in PostgreSQL mode).
  # Set SPRING_PROFILES_ACTIVE=prod when Postgres + secrets are wired.
  PROPERTIES_ENV: local
