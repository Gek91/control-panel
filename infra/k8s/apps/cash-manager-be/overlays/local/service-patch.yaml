apiVersion: v1
kind: Service
metadata:
  name: cash-manager-be
spec:
  # NodePort so the API is reachable via `minikube service cash-manager-be`.
  type: NodePort
  ports:
    - name: http
      port: 8080
      targetPort: http
      nodePort: 30080
      protocol: TCP
