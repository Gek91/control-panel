apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

labels:
  - pairs:
      app.kubernetes.io/name: control-panel-fe
      app.kubernetes.io/part-of: control-panel
    includeSelectors: false

resources:
  - deployment.yaml
  - service.yaml
