apiVersion: v1
kind: Service
metadata:
  name: weight-record-be
spec:
  # NodePort so the API is reachable via `minikube service weight-record-be`.
  type: NodePort
  ports:
    - name: http
      port: 8080
      targetPort: http
      nodePort: 30081
      protocol: TCP
