apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: control-panel

resources:
  - ../../base
  - secret.yaml

images:
  - name: weight-record-api
    newName: weight-record-api
    newTag: 0.0.1

patches:
  - path: deployment-patch.yaml
  - path: service-patch.yaml
  - path: configmap-patch.yaml
