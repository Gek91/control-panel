apiVersion: v1
kind: Secret
metadata:
  name: weight-record-be
  labels:
    app.kubernetes.io/name: weight-record-be
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: control-panel
type: Opaque
stringData:
  # Local/minikube only — mirrors postgres Secret credentials.
  DATABASE_URL: postgresql+psycopg://controlpanel:controlpanel@postgres:5432/controlpanel
